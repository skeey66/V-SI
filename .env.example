# 오케스트레이터 ↔ 에이전트 푸시 콜백 토큰.
# 개발 전용 고정값이다. 실제 값은 커밋하지 않는다(.env 는 .gitignore 대상).
VSI_PUSH_TOKEN=dev-only-token

# 통합 테스트 하네스가 주입하는 시나리오. 비우면 compose 기본값(all_pass)이 쓰인다.
# VSI_SCENARIO=scenarios/all_pass.yaml
