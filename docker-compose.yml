services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vsi
      POSTGRES_PASSWORD: vsi
      POSTGRES_DB: vsi
    ports: ["55432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vsi"]
      interval: 2s
      timeout: 3s
      retries: 20
